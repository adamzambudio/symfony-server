{% extends 'base.html.twig' %}

{% block title %}ComoEnCasa - Tu alojamiento ideal{% endblock %}

{% block header %}{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="text-center mb-5">
        <h1 class="display-4">Bienvenido a <strong>CEC - ComoEnCasa</strong></h1>
        <p class="lead text-muted">Tu próximo alojamiento te está esperando. Encuentra lugares únicos para alojarte con anfitriones de confianza.</p>
        <a href="/ads" class="btn btn-primary btn-lg mt-3">Explorar alojamientos</a>
    </div>

    <h2 class="mb-4">🏠 Alojamientos destacados</h2>
    <div class="row">
        {% for i in 1..3 %}
        <div class="col-md-4 mb-4">
            <div class="card h-100 shadow-sm">
                <img src="https://placehold.co/600x400" class="card-img-top" alt="Apartamento {{ i }}">
                <div class="card-body">
                    <h5 class="card-title">Apartamento moderno {{ i }}</h5>
                    <p class="card-text text-muted">Confort y estilo en cada rincón. Perfecto para viajeros exigentes.</p>
                    <span class="badge bg-secondary mb-2">⭐ 4.8 (23 opiniones)</span><br>
                    <a href="#" class="btn btn-outline-primary btn-sm">Ver más</a>
                </div>
            </div>
        </div>
        {% endfor %}
    </div>

    <h2 class="mb-4 mt-5">👤 Anfitriones destacados</h2>
    <div class="row">
        {% for i in 1..3 %}
        <div class="col-md-4 mb-4">
            <div class="card border-0 shadow-sm">
                <div class="card-body d-flex align-items-center">
                    <img src="https://placehold.co/80x80" class="rounded-circle me-3" alt="Anfitrión {{ i }}">
                    <div>
                        <h6 class="mb-1">Anfitrión {{ i }}</h6>
                        <p class="text-muted small mb-2">Experto en hospitalidad</p>
                        <a href="#" class="btn btn-sm btn-outline-success">Perfil</a>
                    </div>
                </div>
            </div>
        </div>
        {% endfor %}
    </div>
</div>
{% endblock %}

{% block footer %}